set -e

# If running the main server command, automatically prepare the database schema
if [ "$1" = "./bin/thrust" ] || [ "$1" = "bin/rails" ]; then
  echo "Preparing database..."
  bin/rails db:prepare
fi

exec "$@"
